# English Tutor Telegram Bot

Telegram-бот для изучения английского языка с функциями распознавания речи и синтеза речи.

## Возможности

- 🗣️ Распознавание голосовых сообщений (Whisper)
- 🔊 Синтез речи (Google Cloud TTS)
- 💬 Диалоговый режим для практики английского
- 💳 Интеграция платежей (USDT)
- 📊 Отслеживание прогресса пользователей

## Технологический стек

- Python 3.11
- aiogram 3.24.0
- OpenAI Whisper
- Google Cloud Text-to-Speech
- SQLite
- FFmpeg

## Деплой на fps.ms

### Требования

- Аккаунт на [fps.ms](https://fps.ms)
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))
- Google Cloud credentials для TTS (опционально)
- API ключи для Pexels/Unsplash (опционально)

### Шаги деплоя

1. **Подключите ваш GitHub репозиторий к fps.ms**
   ```
   Repository: https://github.com/Denis-Shev777/English_tutor
   Branch: main (или ваша основная ветка)
   ```

2. **Настройте переменные окружения в панели fps.ms**

   Обязательные:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   ```

   Опциональные:
   ```
   GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json
   PEXELS_API_KEY=ваш_ключ
   UNSPLASH_ACCESS_KEY=ваш_ключ
   USDT_WALLET_ADDRESS=ваш_адрес_кошелька
   ```

3. **Загрузите Google Cloud credentials (если используете)**
   - В панели fps.ms перейдите в раздел "Files"
   - Загрузите JSON файл с credentials как `credentials.json`

4. **Запустите деплой**
   - fps.ms автоматически обнаружит Dockerfile
   - Процесс сборки и деплоя запустится автоматически
   - Бот будет доступен после успешного деплоя

### Проверка работы

После деплоя проверьте логи в панели fps.ms:
```
🤖 Бот запущен и готов к работе!
```

Отправьте `/start` вашему боту в Telegram для проверки.

## Локальная разработка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Denis-Shev777/English_tutor.git
   cd English_tutor
   ```

2. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Создайте `.env` файл на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```

5. Заполните `.env` файл вашими токенами и ключами

6. Запустите бота:
   ```bash
   python main.py
   ```

## Структура проекта

```
English_tutor/
├── handlers/           # Обработчики команд и сообщений
│   ├── commands.py    # Команды бота
│   ├── conversation.py # Диалоговый режим
│   ├── payments.py    # Платежи
│   ├── onboarding.py  # Онбординг пользователей
│   ├── keyboards.py   # Клавиатуры
│   └── start.py       # Стартовый обработчик
├── services/          # Сервисы
│   ├── ollama_service.py    # AI сервис
│   ├── tts_service.py       # Text-to-Speech
│   ├── whisper_service.py   # Speech-to-Text
│   └── bscscan_service.py   # Проверка платежей
├── main.py           # Точка входа
├── config.py         # Конфигурация
├── database.py       # Работа с БД
├── logger.py         # Логирование
└── requirements.txt  # Зависимости
```

## Поддержка

При возникновении проблем создайте Issue в репозитории.
